# CloudFront distribution removed — origin S3 bucket (mediconnect-frontend-v1) no longer exists.
# Frontend is now served via Cloud Run / other deployment.
# The cloud resource (E2ZU49H6L7GQPU) should be deleted manually or via terraform state rm.
