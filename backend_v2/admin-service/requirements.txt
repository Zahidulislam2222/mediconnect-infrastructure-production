fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.6.4
boto3==1.34.69
# Security: JWT auth middleware (matches DICOM service pattern)
python-jose[cryptography]==3.3.0
httpx==0.27.0
# Distributed rate limiting (optional — falls back to in-memory if unavailable)
redis==5.0.3
