---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  labels:
    app: doctor-service
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: "50%"
      maxUnavailable: 1
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: doctor-sa
      
      # 🛡️ POD SECURITY (HIPAA Requirement)
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000

      containers:
        - name: doctor-service
          # 🛡️ CONTAINER SECURITY (Block Root Access)
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
          
          image: __IMAGE_URI__
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "8082"
            - name: API_PUBLIC_URL
              value: "__PUBLIC_DOMAIN__" 
            - name: AWS_REGION
              value: "__AWS_REGION__"
            - name: AWS_ROLE_ARN
              value: "__AWS_ROLE_ARN__"
            - name: AWS_WEB_IDENTITY_TOKEN_FILE
              value: "__TOKEN_FILE__"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/var/run/secrets/gcp/gcp-wif-config.json"

          volumeMounts:
            - name: gcp-wif-volume
              mountPath: "/var/run/secrets/gcp"
              readOnly: true

          resources:
            requests:
              cpu: "10m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 180
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 10

      volumes:
        - name: gcp-wif-volume
          configMap:
            name: gcp-wif-config
            optional: true
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-service
spec:
  type: ClusterIP
  selector:
    app: doctor-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: doctor-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: doctor-service
  minReplicas: __MIN_REPLICAS__
  maxReplicas: __MAX_REPLICAS__
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: doctor-service-pdb
spec:
  minAvailable: 50%
  selector:
    matchLabels:
      app: doctor-service