[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["routers", "services", "middleware", "utils"]
omit = ["tests/*", "*/venv/*"]

[tool.coverage.report]
show_missing = true
fail_under = 30
